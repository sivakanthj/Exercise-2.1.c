# Exercise-2.1.c
#include <stdio.h>
void main()
{
    float a, b, c ;
    printf("what is A in equation : ");
    scanf("%f",&a);
    printf("what is B in equation : ");
    scanf("%f",&b);
    printf("what is the constant in equation : ");
    scanf("%f",&c);
    printf("the equation is : %fx + %fy = %f",a,b,c);

}
